<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="10dp"
    android:layout_marginTop="10dp"
    android:backgroundTint="@color/primaryColor"
    android:elevation="5dp"
    android:foreground="@drawable/custom_ripple_effect"
    android:foregroundTint="@color/secondaryColor"
    app:cardCornerRadius="5dp"
    app:contentPadding="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="5"
            android:orientation="vertical">

        <!-- Title -->
        <TextView
            android:id="@+id/recyclerview_appointment_title_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:autoSizeMaxTextSize="25sp"
            android:autoSizeTextType="uniform"
            android:drawablePadding="10dp"
            android:maxLines="1"
            android:text="@string/title"
            android:textColor="@color/white"
            android:textSize="25sp"
            app:drawableStartCompat="@drawable/ic_title_white"/>


        <!-- Date -->
        <TextView
            android:id="@+id/recyclerview_appointment_date_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:autoSizeMaxTextSize="20sp"
            android:autoSizeTextType="uniform"
            android:drawablePadding="10dp"
            android:maxLines="1"
            android:text="@string/appointment_date"
            android:textColor="@color/white"
            android:textSize="20sp"
            app:drawableStartCompat="@drawable/ic_calendar_white"/>

        <!-- Date -->
        <TextView
            android:id="@+id/recyclerview_appointment_with_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:autoSizeMaxTextSize="20sp"
            android:autoSizeTextType="uniform"
            android:drawablePadding="10dp"
            android:maxLines="1"
            android:text="@string/who"
            android:textColor="@color/white"
            android:textSize="20sp"
            app:drawableStartCompat="@drawable/ic_person_white"/>

    </LinearLayout>

        <ImageButton
            android:id="@+id/recyclerview_appointment_options_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/custom_ripple_effect"
            android:contentDescription="@string/edit_appointment"
            android:src="@drawable/ic_options_white" />

    </LinearLayout>
</androidx.cardview.widget.CardView>